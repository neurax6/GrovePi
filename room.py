__author__ = 'neuraxis'

class Room(object):

    def __init__(self,p_sensor=None,p_table=None,p_room=None)
        if not p_sensor:
            p_sensor = []
        if not p_table:
            p_sensor = []
        if not p_room:
            p_sensor = []
        self.__sensor = p_sensor
        self.__table = p_table
        self.__room = p_room